package com.AttendEase_beta.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.AttendEase_beta.model.IML;

public interface IMLRepository extends JpaRepository<IML, Integer> {
}
